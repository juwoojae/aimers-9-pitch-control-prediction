# Project instructions

이 저장소에서 작업하는 사람과 AI 에이전트는 작업 전 `README.md`와 `docs/01_problem_definition.md`부터 읽는다.

## 절대 조건

- 예측 기준 시점은 **현재 투구가 이루어지기 직전**이다.
- 현재 투구의 결과, 공의 최종 위치, 판정, 타격 결과 및 현재 투구 이후 정보는 피처로 사용하지 않는다.
- 이력 피처는 현재 행을 제외하고(`shift(1)`과 동등한 의미) 과거 데이터만으로 계산한다.
- 전처리, 인코딩, 집계, 결측치 대체, 확률 보정은 각 validation fold의 학습 구간으로만 fit한다.
- 평가 데이터 전체의 통계나 순서를 학습 피처 생성에 사용하지 않는다.
- 원본 데이터는 수정하지 않는다. 파생 산출물은 원본과 분리한다.

## 작업 방식

- 확인하지 않은 데이터 컬럼·파일명·정렬 순서를 가정하지 않는다.
- 새 컬럼을 사용하기 전에 `docs/04_data_dictionary.md`에 의미와 시점 판정을 기록한다.
- 모델 비교는 대회 공식 Brier Skill Score와 동일한 구현을 우선 사용한다.
- 모든 실험은 seed, split, feature set, metric, runtime을 남긴다.
- 모델 성능만큼 확률 calibration과 fold 간 안정성을 확인한다.
- 제출 코드는 네트워크 없이 저장소/압축 파일 내부 자원만으로 실행되어야 한다.
- 경로는 제출 압축 파일 루트 기준 상대 경로를 사용한다.
- 한국어로 문서화하고 코드 식별자는 영어를 사용한다.

## 문서 갱신 규칙

- 공식 규정이나 데이터에서 확인한 사실은 `확정`으로 기록한다.
- 아직 검증하지 않은 모델링 아이디어는 `가설`로 표시한다.
- 규칙 변경이나 중요한 선택은 `docs/05_decision_log.md`에 날짜와 근거를 남긴다.
